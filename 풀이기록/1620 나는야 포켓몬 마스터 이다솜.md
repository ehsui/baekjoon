https://www.acmicpc.net/problem/1620
#done 
# 공부한 개념

[Set, Map](obsidian://open?vault=%EB%A8%B8%EB%A6%BF%EC%86%8D&file=02.%20Area%2F%EA%B3%B5%EB%B6%80%2F%EC%9E%90%EB%A3%8C%EA%B5%AC%EC%A1%B0%2FList%20%26%20Set%20%26%20Map)

[입출력 속도에 관한](obsidian://open?vault=%EB%A8%B8%EB%A6%BF%EC%86%8D&file=02.%20Area%2F%EA%B3%B5%EB%B6%80%2FC%2B%2B%2FSTL)

# 설명

이 문제는 map, set 등의 자료구조와 STL에 대한 이해가 부족해 여러 번 틀린 문제다. map, set에 대한 개념 숙지 이후에도 c++의 입출력 속도에 관한 내용 공부가 필요했다.

# 코드

```cpp
#include <iostream>
#include <string>

#include <unordered_map>

using namespace std;

unordered_map <int, string> Poket;
unordered_map <string, int> Poket2;


int main() {
	int N, M;

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	cin >> N >> M;

	string name;

	for (int i = 1; i <= N; i++) {
		cin >> name;
		Poket.insert({i, name});
		Poket2.insert({ name, i });
	}

	string q;
	unordered_map <int, string>::iterator it;
	unordered_map <string, int>::iterator it2;
	for (int i = 0; i < M; i++) {

		cin >> q;

		if (isdigit(q[0])) 
		{
			it = Poket.find(stoi(q));

			cout << it->second << '\n';
		}
		else
		{
			it2 = Poket2.find(q);
			cout << it2->second << '\n';
		}

	}
}

```

